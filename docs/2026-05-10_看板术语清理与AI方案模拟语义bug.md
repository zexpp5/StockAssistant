# 2026-05-10 · 看板术语清理 + AI 方案模拟语义 bug 发现

接续 [2026-05-10 数据迁移与方案实盘跟踪](2026-05-10_数据迁移与方案实盘跟踪.md)。今天主要是**用户视角**的复盘 —— 用新人眼光过一遍看板，把误导的术语和虚假数字全部拔掉，并发现一个 AI 方案模拟 panel 的 look-ahead bias。

## 一句话

> 看板里有 3 处**严重误导新人**的设计被修了，但还留了一个**语义 bug** 待明天修：AI 方案模拟用的是"今天最新的 12 只 + 5-08 锚定 NAV=100"，存在 look-ahead bias。

## 已改动

### 1. 「升级建议」tab 里的「v7 全栈整合」对比表 → 删除假数字 + 重写为「两条线」实际架构

**问题**：[stock_dashboard.html:3996-3998](../stock_dashboard.html#L3996-L3998) 之前展示一张「全程进步实测」表，列出：

```
v7 全栈整合 | 10 因子 | YTD +51.5% | Alpha +43.8% | 胜率 79%
```

但 grep 全仓库找不到任何来源 —— **整个 +51.5%/+43.8%/79% 是硬编码到 HTML 里的"愿景数字"**，没有 backtest、没有 reverse_validation_v7.json、没有 reverse_validate_v7.py。下面那行小字虽写"⚠️ backtest 非未来预测"，**但实际连 backtest 都没跑过**。

同样问题：

- 「**双引擎自动刷新**」表里列了 `intraday_refresh.sh`（盘中每 30 分钟 v7 重算）—— **该脚本不存在**，是规划文案
- 「**v7 模型 — 10 个因子**」block 描述的合成模型 —— **代码里也不存在**

**改动**：把这 3 个 block（[build_stock_dashboard_html.py:1081-1145](../build_stock_dashboard_html.py#L1081-L1145) 区域）整段替换为「**🏗 系统实际架构 — 两条线、五个部件**」：

- 顶部黄条提醒「v 编号是部件标签，不是版本升级」+ 手机零件类比
- 🇺🇸 美股线表格：v6 选股 + v7 防御 + v7.5 情报（实际每天 07:30 在跑）
- 🇨🇳 A 股线表格：v9 选股 + v8 事件（实际工作日 16:30 在跑）
- 公共支撑：13F、enrichment、DuckDB、AI 方案模拟
- 已规划未做：v7 10 因子合成、intraday、B 路线 Phase 2-4

### 2. 「方案回测」tab → 「🤖 AI 方案模拟」

旧名「方案回测」误导（实际是 forward 跟踪），中间过渡为「实盘跟踪」（"实盘"暗示真金白银）后用户仍困惑，最终定为 **「🤖 AI 方案模拟」**。

H2 也加了 ⚠️ 提醒 —— [stock_dashboard.html:3786](../stock_dashboard.html#L3786)：

```
🤖 AI 方案模拟：v6 推荐 12 只从锁定日起的每日真实表现
⚠️ 这不是你的真实账户，是"假设你 5-08 收盘按方案推荐买入了那 12 只、之后一直持有"的模拟跟踪。
```

替换覆盖了 16 处（.py 8 处 + .html 8 处）：tab nav、H2、section 注释、JS 注释、Python print、确认对话框文案。

### 3. 「我的持仓」一键加载按钮 → 加意图警告 + 改名 + 改色

[build_stock_dashboard_html.py:707](../build_stock_dashboard_html.py#L707)：

| 项 | 改前 | 改后 |
|---|---|---|
| 文案 | 🎯 一键加载方案 A v6（学术因子）| 📋 把方案抄进持仓（仅在你真下单后用）|
| 颜色 | bg-emerald-600（暗示推荐操作）| bg-amber-600（琥珀色，暗示谨慎）|
| 点击行为 | 直接加载 | 先弹意图说明 confirm（说明该按钮是"真买入后批量录入"用的，不是"想看方案表现就点"）|
| 完成后 alert | 列出 Sharpe/收益/波动 | 改为提醒「🚨 买入价目前 = 今天现价。请逐行点【编辑】改成你真实成交价」 |

理由：用户原本以为按一下就能"模拟跟踪方案表现"，但这个按钮的实际行为是把"今天现价当买入价"塞进 localStorage，造成**「我的持仓」面板和「AI 方案模拟」面板内容重复**——而正确做法是**只看 AI 方案模拟**，「我的持仓」留给真实买卖后用。

## 概念上的澄清（用户视角的关键收获）

新人盯着看板会困惑两件事，今天把它们写到看板首屏文案里了：

### 「v 编号是部件标签，不是版本升级」

| 部件 | 管什么 |
|---|---|
| v1 | 美股老式主观打分（基本不用）|
| v6 | **美股选股引擎**（4 学术因子 + Markowitz）|
| v7 | 美股防御层（VIX/200MA/-15% 止损/PCR）|
| v7.5 | 美股 OpenBB 综合情报 |
| v8 | A 股事件层（IPO/解禁/政策）|
| v9 | A 股选股引擎（6 因子）|

**没有"最新版本"** —— 它们是不同部件，同时跑、互不替代。新人会以为 v9 是 v8 的升级，是误读。

### 「AI 方案模拟 ≠ 我的持仓」

| 面板 | 答什么问题 | 给谁用 |
|---|---|---|
| 🤖 AI 方案模拟 | "**系统的策略好不好用**？" | 只看这个就够了（paper trading 阶段）|
| 💼 我的持仓 | "**我账户里的钱赚了多少**？" | 真下单了才填（按真实成交价）|

对**完全跟随系统推荐**的用户来说两者基本等价——这是面板设计上的冗余。已通过文案 + 按钮警告引导用户在 paper trading 阶段**只看 AI 方案模拟**。

## 待办（明天起）

### P0 — 修 AI 方案模拟的 look-ahead bias（~1 小时）

**问题**：[build_stock_dashboard_html.py::compute_plan_forward_track()](../build_stock_dashboard_html.py#L3718)

```python
# 当前实现：
plan_list = plan.get("plan_v5") or plan.get("plan_v6") or plan.get("plan") or []
inception_date = _find_plan_inception_date()  # MIN(ts) from DuckDB → 永久不变
```

`plan` 是函数入参，传入的是**今天最新** `plan_a_v5.json` 的内容；`inception_date` 永久冻结在第一次写库的日期（5-09）。

**带来的 bug**：如果明天 v6 选股把 MU 换成 AAPL，AI 方案模拟会**把 AAPL 从 5-08 起的真实历史轨迹算进 NAV**，仿佛"你 5-08 就买了 AAPL"——这是 look-ahead bias，不可实操。

**修法（P0 最小改动）**：把 tickers 也冻结到 inception 那天的 plan 快照。

```python
# 修后：
inception_plan = _load_inception_plan_from_duckdb()  # 读 DuckDB 里 inception 那天的 plan
plan_list = inception_plan.get("plan_v5") or ... or []
inception_date = _find_plan_inception_date()
```

**结果**：从 buy-and-hold 视角忠实跟踪"5-08 那天锁定的 12 只"，没有 look-ahead bias。但**没有动态调仓的价值**（A 类设计）。

### P1 — 加「动态 rebalance」模式（~1-2 天）

DuckDB `data/snapshots/optimize/` 已经累积了 plan_v6 历史快照（5-09 起每天都有）。利用这些快照模拟"按 v6 当时的最新推荐每周 rebalance"的 NAV 演化（C 类设计）：

- 决定 rebalance 频率：每周一收盘按当周最新 plan 调仓
- 估算 transaction cost：bid-ask spread 假设 5 bps + 印花税（A 股 0.05% / 美股 0.001%）
- 输出"每次调仓的盈亏归因"表

### P2 — 双曲线对比（~半天）

在「AI 方案模拟」panel 同时画两条 NAV 曲线：

- **A 线（buy-and-hold）**：5-08 锁定的 12 只死守不动
- **C 线（动态 rebalance）**：每周按 v6 当周最新推荐调仓

差距 `C - A` = **AI 动态选股的真实价值**（用户最该看的指标）。

理由：

- 光看 A 会让用户以为"AI 选完就行不用动" —— 否定了模型的核心价值
- 光看 C 会让用户高估 AI 智能 —— 不知道有多少回报来自 5-08 那次幸运

两条对照才暴露 AI 的真实增量。

### 长期跟踪项（不急）

1. **plan_a_v5.py / plan_a_v5.json 改名为 _v6**：v5→v6 升级时只改了看板文案，文件名留尾巴
2. **看板「📡 系统响应能力」section** 还提到「daily 批处理 + 盘中 30 分钟轮询」「盘中异动告警（每 30 分钟）」—— 同样指向不存在的 intraday 引擎，需要清理
3. **「我的持仓」自动加载逻辑**：[build_stock_dashboard_html.py:1593](../build_stock_dashboard_html.py#L1593) 在 `swapDataSource()` 里 auto-call `loadPlanAv6()`。切换数据源不应该重置用户持仓，应该删

## 提交记录

均在 `M build_stock_dashboard_html.py` `M stock_dashboard.html` 里，**未 commit**。明天 P0 修完一起推：

```
2026-05-10 看板术语清理 + AI 方案模拟 bug 修复（P0）
- 删除 v7 全栈整合假回测数字 + 重写为「两条线、五个部件」架构图
- 「方案回测」→「AI 方案模拟」全仓 16 处替换
- 「一键加载方案」按钮加意图警告 + 改色 + 改文案
- compute_plan_forward_track 冻结 inception tickers，消除 look-ahead bias
```

## 关键代码位置

| 位置 | 用途 |
|---|---|
| [build_stock_dashboard_html.py:418](../build_stock_dashboard_html.py#L418) | tab nav 「🤖 AI 方案模拟」 |
| [build_stock_dashboard_html.py:707](../build_stock_dashboard_html.py#L707) | 「📋 把方案抄进持仓」按钮 |
| [build_stock_dashboard_html.py:929](../build_stock_dashboard_html.py#L929) | tab 内 H2 + ⚠️ 提醒 |
| [build_stock_dashboard_html.py:1081-1175](../build_stock_dashboard_html.py#L1081-L1175) | 「🏗 系统实际架构」block（重写后）|
| [build_stock_dashboard_html.py:1934](../build_stock_dashboard_html.py#L1934) | `loadPlanAv6()` JS 函数（加 confirm）|
| [build_stock_dashboard_html.py:3695-3801](../build_stock_dashboard_html.py#L3695-L3801) | `compute_plan_forward_track()` ← P0 要改这里 |
| [data/snapshots/optimize/plan_v6_*.json](../data/snapshots/optimize/) | DuckDB plan_v6 历史快照（P1 用）|
