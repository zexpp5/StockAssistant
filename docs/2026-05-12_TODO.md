# 2026-05-12 TODO（昨日 PM 续作）

> **昨日（2026-05-11 PM）大改造已完成**：产品三层架构（自选股 + AI 推荐 + 持仓）+ 命名重组 + 数据底座迁移 + A 股扩容 25×。
> 本 doc = 明日打开就看到的下一步 polish 清单。
> 配套总账见下方「📋 昨日 PM 完成清单」。

---

## ⏰ 今天（2026-05-12）做什么 · 按优先级

### 🔥 优先做（半天就能完成 + 用户价值明显）

#### 1. 「❌ 不建议」评级档 · 让 AI 评级三档完整
- **现状**：自选股 AI 评级只有 ✅推荐 / ⚠️观察 / — 未评级 三档；缺 ❌
- **原因**：daily_picks_v5 当前只入库 z ≥ 0.5 的"入选"股，z < -0.5 的"负向"股没存
- **要改**：daily_picks_v5 的 picks 表存全部自选股（含 z 负向档），rating 字段加 "⛔ 不建议" 档
- **顺手改**：`_wlRatingBadge`（[build_stock_dashboard_html.py:2256](scripts/pipeline/build_stock_dashboard_html.py#L2256) 附近）识别 "⛔" 输出 ❌ 不建议 红色 badge
- **工作量**：半天

#### 2. 飞书卡片加上周回顾 section
- **现状**：昨天加的 `section_weekly_hitrate()` 只在 morning_brief 的 markdown 输出（[morning_brief.py](stock_research/jobs/morning_brief.py)）
- **没做**：`_build_card_payload()` 的飞书 card v1 schema 里没加这个 section
- **要改**：在 `_build_card_payload()` 里加一个 `section_hitrate_card()` 卡片块，周一才生成
- **工作量**：半天

---

### 📊 中等优先（数据积累 1-2 周后再做更有意义）

#### 3. 评级 alpha vs SPY
- **现状**：reviews 表只算个股 pct，没算超额收益
- **要改**：reviews 表加 `entry_spy_price` / `current_spy_price` / `alpha_pct` 三字段，weekly_review.py 写入
- **工作量**：1 天
- **依赖**：现在做也行，但 N=175 + days_held=0.8 数据少 alpha 噪音大

#### 4. 评级分档命中率 widget
- **位置**：dashboard `#picks-review` section 顶部
- **内容**："⭐⭐⭐ 命中率 X% (n=121) vs ⭐⭐ 命中率 Y% (n=25)" 三个 KPI 卡片
- **工作量**：半天
- **依赖**：等数据 ≥ 2 周后做才有真实信号

---

### 🛠 低优先（polish）

#### 5. A 股市值闸门 currency 换算
- **现状**：`discover_candidates.py` 的 `filter_by_market_cap` 用 5e9 USD 阈值，但 A 股 yfinance 返回的 cap 是 RMB
- **影响**：当前 A 股 5B RMB ≈ 700M USD 就能通过 → 闸门**比预期松**
- **要改**：yfinance Ticker.info 里有 currency 字段，按 currency 换算到 USD 再比
- **工作量**：半天

#### 6. `discover_candidates` 改"周一固定跑"
- **现状**：[daily_refresh.sh:185-198](daily_refresh.sh#L185-L198) 用"6 天 stale"判断（效果近似周频）
- **要改**：加一行 `if [ "$(date +%u)" != "1" ]`（只周一跑）
- **工作量**：10 分钟

#### 7. A 股推荐性能优化
- **现状**：254 只 A 股 + 290 美股 → yfinance 市值查询会跑 5-10 分钟
- **要改**：`filter_by_market_cap` 用 `concurrent.futures.ThreadPoolExecutor` 并发
- **工作量**：半天

#### 8. holdings 实时当前价存 DB
- **现状**：前端 dashboard 用 yfinance 拉持仓股的当前价（前端在线计算）
- **影响**：后端 trade_delta / risk_metrics 计算"当前 weight" 时拿不到现价
- **要改**：每天 fetch_stock_prices 跑完后顺手把 holdings 的 current_price 也算一遍存 DB
- **工作量**：半天

---

## ⚠️ 注意事项 / 已知坑

### A. `stock_db.py` 2026-05-11 PM 又加了 watchlist 字段
- 新增：`earnings` / `verification` / `info_breakdown` 三个 VARCHAR 字段
- 状态：跟我加的 holdings 表 schema **不冲突**
- 提醒：下次改 stock_db.py 时记得 watchlist 现在有 22 列（不是 19 列）

### B. 数据还太新 → 命中率统计现在没意义
- reviews 表 175 条 / 3 天累积 / avg_days_held=0.8 天
- discovery_tracking 几天 / alpha 大多缺失（yfinance 拉不到这么短期数据）
- 等 **2 周后**回头看，命中率才有真实信号
- 现在做"分档命中率 widget"会显示噪音

### C. A 股 universe 第一次跑慢
- daily_refresh.sh 里 discover_candidates 是 6 天 stale，下次自然触发可能要等几天
- 想立刻看效果：手动跑 `python3 scripts/tools/discover_candidates.py`（5-10 分钟）

### D. 持仓 cache 加载时机
- `_ensureHoldingsLoaded()` 现在三处调用：DOMContentLoaded / switchTab portfolio / loadWatchlistTable
- 如果发现「💼 持」标记没显示，先在浏览器 console 检查 `_holdingsCache.length`

---

## 📁 关键文件指针（按昨日改动）

### 新文件
- [stock_research/core/a_share_universe.py](stock_research/core/a_share_universe.py) — A 股扫描池（方案 B: HS300 科技子集 + 科创 50 + 创业板指 = 220 只）

### 改动文件
- [scripts/lib/stock_db.py](scripts/lib/stock_db.py) — holdings 表 + 6 个 CRUD 函数（fetch_all_holdings / insert / update / delete / bulk_replace_holdings / get_holding）
- [stock_research/api/main.py](stock_research/api/main.py) — 5 个 /api/holdings endpoint
- [scripts/pipeline/build_stock_dashboard_html.py](scripts/pipeline/build_stock_dashboard_html.py) — 侧边栏重组 + AI 评级列 + 持仓 inline badge + 加自选按钮 + 命名替换 + cache 改造
- [scripts/pipeline/trade_delta.py](scripts/pipeline/trade_delta.py) — 删 CURRENT_PLAN_A 硬编码 + 加 load_current_from_holdings + _infer_fx_to_rmb
- [scripts/tools/discover_candidates.py](scripts/tools/discover_candidates.py) — build_universe 接入 A 股增强源
- [stock_research/jobs/morning_brief.py](stock_research/jobs/morning_brief.py) — 加 section_weekly_hitrate（周一专属）

### 新增 JS 函数（dashboard）
- `_heldBadge(code)` — 持仓「💼 持」标记
- `_wlRatingBadge(code)` — AI 评级 ✅⚠️
- `addDiscoveryToWatchlist(ticker, btn)` — 一键加自选
- `_markDiscoveryAddedButtons()` — 标记已加自选的 AI 推荐
- `_ensureHoldingsLoaded()` — 持仓 cache 异步加载
- `refreshHoldingsAndRender()` — 增删改后重新拉 + render

---

## 📋 昨日 PM 完成清单（fyi）

| # | 项 | 类型 |
|---|---|---|
| 1 | 行业调研定命名（自选股 + AI 推荐 + 加自选） | 新增 |
| 2 | AI 推荐表格加「➕ 加自选」按钮 | 新增 |
| 3 | 全站 UI 文案改名（候选发现→AI 推荐 / 每日优选→自选股·今日 Top） | 新增 |
| 4 | 侧边栏 5 大分类重组（旧"今天/发现/验证" → "我的池子/AI 助手/深度研究/管理/投资框架"） | 新增 |
| 5 | Emoji 体系无重复 + 字体层级（大类 16px / 子项 13px 缩进） | 新增 |
| 6 | 持仓 localStorage → DuckDB（5 个 API + 自动迁移） | 新增 |
| 7 | trade_delta 改读真实持仓（删硬编码 CURRENT_PLAN_A） | 新增 |
| 8 | 自选股加 AI 评级列（✅⚠️/未评级 三档） | 新增 |
| 9 | 自选股加「💼 持」标记（同 code 多笔自动聚合） | 新增 |
| 10 | morning_brief 加 section_weekly_hitrate（周一专属） | 新增 |
| 11 | A 股 universe 扩容 25×（10 → 254 只） | 新增 |
| 12 | AI 评级回溯（reviews 表 + picks-review section） | 已存在,确认 |
| 13 | 因子调权（calibrate_pick_weights + factor_weights.json） | 已存在,确认 |
| 14 | AI 推荐周频（daily_refresh 6 天 stale） | 已存在,确认 |
| 15 | 修 bug：链条筛选下拉框为空（cache 时机错） | 修复 |
| 16 | 修 bug：_ratingBadge 同名函数冲突 | 修复 |

**核心闭环全部跑通**：自选 + AI 推荐 + 一键加自选 + 评级 + 持仓 + 回溯。剩下都是 polish。

---

## 🚀 起手建议

明天打开时**先做 #1 + #2**（共 1 天工作量）— 这两件做完用户能看到的产品完整度跳一档：
- AI 评级三档完整（✅⚠️❌） · 不再只有 + 没有 -
- 飞书卡片周一推回顾 · 不进 dashboard 也能看 AI 准不准

做完再看数据积累状态，决定要不要做 #3/#4。
