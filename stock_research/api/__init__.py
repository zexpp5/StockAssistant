"""FastAPI 服务层（预留）。

启动：
  pip install fastapi uvicorn
  uvicorn stock_research.api.main:app --reload --host 0.0.0.0 --port 8000

部署：
  uvicorn stock_research.api.main:app --workers 4 --host 0.0.0.0 --port 80
"""
